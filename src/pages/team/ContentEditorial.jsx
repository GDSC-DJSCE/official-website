import React from "react";
import { TeamNav, THead, TCard } from "../../components";

export const ContentEditorial = () => {
  return (
    <div>
      <THead />
      <TeamNav />
    </div>
  );
};
